version: '3'
services:
  plantuml-editor:
    image: hakenadu/plantuml-editor:0.4.0-beta
    container_name: plantuml-editor
    build:
      context: ../../../
      dockerfile: ./build/variants/adoptopenjdk/Dockerfile
    ports:
    - 80:8080